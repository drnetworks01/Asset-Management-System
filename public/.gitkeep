# Empty placeholder so the directory exists in git.
# Next.js's standalone build COPYs from /app/public; without this dir, the
# Dockerfile's `COPY --from=builder /app/public ./public` fails with "not found".
